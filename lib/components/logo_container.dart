import 'package:flutter/material.dart';

/// container with logo for animation level 4
class LogoContainer extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      width: 100,
      height: 100,
      child: Image.asset("assets/software.png"),
    );
  }
}
